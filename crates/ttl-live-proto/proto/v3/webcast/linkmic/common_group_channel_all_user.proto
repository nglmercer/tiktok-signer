syntax = "proto3";

package webcast.linkmic.common;

import "webcast/linkmic/common_group_channel_user.proto";

message GroupChannelAllUser {
  int64 group_channel_id = 1;
  repeated GroupChannelUser user = 2;
  int64 content_version = 3;
}
